name: api responding
description: api is responding with 200
request:
  method: GET
  url: /pets/1
  matchers:

response:
  body:
    id: 1
  matchers:
    body:
        - path: name
          type: by_regex
          value: .*
  status: 200